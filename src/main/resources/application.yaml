spring:
  application:
    name: waypoint

---

spring:
  config:
    activate:
      on-profile: "!local"
    import: ${SPRING_CONFIG_IMPORT:optional:configserver:https://spring.nowstart.org/config}
